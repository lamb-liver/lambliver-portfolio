# 協作指示

- 以繁體中文回報。
- 沿用靜態 TypeScript 資料與既有元件，不新增 CMS、資料庫或自製 runner。
- 字級只用 Display／H2／H3／Body／Meta；mono tracking 固定 0.14em。
- 圓角：卡與按鈕直角；控件（nav、chip、theme toggle）用 rounded-sm。
- 完成前執行 `npm run lint`、`npm run build`、`git diff --check`。
- 視覺改動另檢查 390px／1440px 與 Light／Dark。
